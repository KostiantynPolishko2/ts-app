import React, { FC, ReactElement } from 'react';
import { Link } from 'react-router-dom'

const Home: FC = (): ReactElement => {
    return(
        <div>
            Home page
        </div>
    );
}

export default Home;