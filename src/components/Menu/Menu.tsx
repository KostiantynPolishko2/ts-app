import React, { FC } from 'react';
import { MenuWrapper } from './Menu.styled';

interface MenuProps {}

const Menu: FC<MenuProps> = () => (
   <MenuWrapper>
      Menu Component
   </MenuWrapper>
);

export default Menu;
